import * as ping from "./ping";
import * as balance from "./balance";
import * as send from "./send";
import * as help from "./help";
import * as generate from "./generate";

export const commands = {
  ping, balance, send, help, generate,
};